{
  "name": "acme-api",
  "version": "1.0.0",
  "dependencies": {
    "@redocly/cli": "1.0.0-beta.114",
    "redoc": "^2.0.0"
  },
  "private": true,
  "scripts": {
    "start": "redocly preview-docs",
    "build": "redocly build-docs && mv redoc-static.html docs && cd docs && rm index.html && mv redoc-static.html index.html",
    "test": "redocly lint"
  }
}
