get:
  tags:
    - Customer
  summary: List Representative
  description: Returns a JSON with all Representatives
  security:
    - Token: []
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: string
          examples:
            response:
              value:
                {
    "$id": "40478",
    "$values": [
        {
            "$id": "40479",
            "ID": 561,
            "Codigo": 1,
            "Descricao": "SCHUSTER COMERCIO DE EQUIPAMENTOS ODONTOLOGICOS LTDA",
            "Email": "\tschuster@schuster.ind.br"
        },
              ]
              }
    "401":
      description: Unauthorized
      content:
        text/plain:
          schema:
            type: string
          examples:
            response:
              value:
                Unauthorized (Token not found on request)