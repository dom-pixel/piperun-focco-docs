get:
  tags:
    - Budget
  summary: Payment Terms
  description: List the payment terms.
  security:
    - Token: []
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: string
          examples:
            response:
              value:
                {
    "$id": "64",
    "$values": [
        {
            "$id": "65",
            "ID": 593,
            "Codigo": 1,
            "Descricao": "A VISTA BB"
        }
    ]
}
    "401":
      description: Unauthorized
      content:
        text/plain:
          schema:
            type: string
          examples:
            response:
              value:
                Unauthorized (Token not found on request)